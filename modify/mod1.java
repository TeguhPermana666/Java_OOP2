package OOP.modify;

public class mod1 {
    /*
    modify tu ada 2
    
    +acces modfy->dapat digunakan control acces 

    =>class
        ->public->	The class is accessible by any other class

        ->default-> The class is only accessible by classes in the same package.
         This is used when you don't specify a modifier.
         You will learn more about packages in the Packages chapter

    =>construct,atribut,mehode
        ->public->The code is accessible for all classes

        ->private->The code is only accessible within the declared class

        ->default->The code is only accessible in the same package.
         This is used when you don't specify a modifier. 
         You will learn more about packages in the Packages chapter

        ->protected->The code is accessible in the same package and subclasses.
         You will learn more about subclasses and superclasses in the Inheritance chapter

    +non-acces modfy-> tidak dapat digunakan untuk control acces tapi menyediakan fungsi yang lain
    =>class
        ->final->The class cannot be inherited by other classes 

        ->abstrak->The class cannot be used to create objects
         (To access an abstract class, it must be inherited from another class)
    
    =>construct,atribut,mehode
        ->final->Attributes and methods cannot be overridden/modified

        ->abtsrak->Can only be used in an abstract class, and can only be used on methods.
         The method does not have a body, for example abstract void run();.
         The body is provided by the subclass (inherited from).

        ->static->Attributes and methods belongs to the class, rather than an object
    */
}
